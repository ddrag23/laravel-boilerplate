const elid = (element) => document.getElementById(element)
const elqs = (element) => document.querySelector(element)
const elall = (element) => document.querySelectorAll(element)

export { elid, elqs, elall }